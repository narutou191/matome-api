services:
  - type: web
    name: matome-api
    runtime: python-3.11
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn matome_api:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
